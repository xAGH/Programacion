cvtColor(src, code) => Agrega una gama de colores a una imagen, se puede 
usar para transformar a  escala de grises.

threshold(src, tresh, maxval, type) => Se utiliza para aplicar el umbral. 
Retorna dos valores, el umbral utilizado y la imagen con el umbral aplicado.

findContours(umbral, mode, method) => Encuentra el contorno de una imagen.
Retorna dos valores, los contornos y la jerarquia de estos.

GaussianBlur(img, ksize, sigmaX) => Suaviza o elimina parcialmente los 
ruidos la imagen dada dependiendo de un método especificado.

Canny(img, threshold1, threshold2) => Elimina el ruido de las imagenes y 
ayuda a marcar contornos.

morphologyEx(img,op, kernel) => Elimina o cambia la imagen dependiendo el
método entregado(apertura, clausura, etc).
